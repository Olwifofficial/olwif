import {sqliteTable,text,integer,real,primaryKey,index} from "drizzle-orm/sqlite-core";
export const members=sqliteTable("members",{id:text("id").primaryKey(),preferences:text("preferences").notNull(),createdAt:integer("created_at").notNull()});
export const savedReports=sqliteTable("saved_reports",{memberId:text("member_id").notNull(),reportId:text("report_id").notNull(),createdAt:integer("created_at").notNull()},t=>[primaryKey({columns:[t.memberId,t.reportId]})]);
export const memberWatches=sqliteTable("member_watches",{memberId:text("member_id").notNull(),tokenKey:text("token_key").notNull(),reportId:text("report_id").notNull(),quantity:real("quantity"),costBasisUsd:real("cost_basis_usd"),monitorEnabled:integer("monitor_enabled",{mode:"boolean"}).notNull().default(true),baseline:text("baseline").notNull(),createdAt:integer("created_at").notNull()},t=>[primaryKey({columns:[t.memberId,t.tokenKey]}),index("watches_token").on(t.tokenKey)]);
export const memberAlerts=sqliteTable("member_alerts",{id:text("id").primaryKey(),memberId:text("member_id").notNull(),title:text("title").notNull(),body:text("body").notNull(),reportId:text("report_id").notNull(),createdAt:integer("created_at").notNull(),read:integer("read",{mode:"boolean"}).notNull().default(false)},t=>[index("alerts_member_time").on(t.memberId,t.createdAt)]);
export const monitorTokens=sqliteTable("monitor_tokens",{tokenKey:text("token_key").primaryKey(),address:text("address").notNull(),chain:text("chain").notNull(),reportId:text("report_id").notNull(),snapshot:text("snapshot").notNull(),lastCheckedAt:integer("last_checked_at").notNull(),lastAttemptAt:integer("last_attempt_at").notNull().default(0),leaseUntil:integer("lease_until").notNull().default(0)});
